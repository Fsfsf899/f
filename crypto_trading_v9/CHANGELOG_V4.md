# V3 → V4: المشاكل التي أُصلحت

## أخطاء حقيقية اكتُشفت وأُصلحت

| # | المشكلة في V3 | الأثر | الإصلاح |
|---|---|---|---|
| 1 | `SupportResistance` تحسب من نافذة كاملة وتُسقطها على التاريخ | **نظر للأمام** — نتائج الباكتست وهمية | `causal_levels()` مع `confirmation_index` لكل محور |
| 2 | `stoch_rsi` يستخدم `np.nanmean` على المصفوفة كاملة | **نظر للأمام** — اكتشفه الاختبار الآلي | `_causal_sma()` من القيم السابقة فقط |
| 3 | `int(score*2) % 6` للنجوم | score 3.0→0★ بينما 2.5→5★ — التفاف عكسي | خريطة عتبات رتيبة، مختبَرة على 2000 قيمة |
| 4 | `confidence = len(conditions)/8` يُعرض كاحتمال | رقم بلا معنى إحصائي | فصل تام + معايرة Isotonic/Platt + Brier |
| 5 | الخروج من `close[i]` فقط | يفوّت الوقف الذي ضُرب داخل الشمعة | `resolve_long_exit()` من High/Low |
| 6 | لا سياسة للشمعة المزدوجة | افتراض ضمني لأفضل نتيجة | 3 سياسات، الافتراضي conservative |
| 7 | الفجوات تُنفَّذ عند المستوى لا عند الفتح | خسائر أقل من الحقيقة | `gap_adjust()` |
| 8 | المركز المفتوح يختفي عند نهاية الباكتست | إحصائيات ناقصة | إغلاق صريح بسبب `BACKTEST_END` |
| 9 | 7 محركات باكتست متنافسة | نتائج متضاربة | محرك واحد: `src/backtest/engine.py` |
| 10 | Walk-Forward = تقسيم بنفس المعاملات | لا يكشف Overfitting | تدريب → تجميد → OOS، مع فحص التداخل |
| 11 | الحد اليومي مقابل الرصيد الحالي | يتمدد مع كل خسارة | `daily_starting_equity` ثابت |
| 12 | حجم المركز = risk / stop_distance | يتجاهل الرسوم والانزلاق | مخاطرة فعلية — أعلى بـ ~18% |
| 13 | `np.random` في 11 ملفاً منها `live_monitor.py` | ملف باسم Live يولّد أسعاراً وهمية | كل التوليد في `tests/fixtures.py` فقط |
| 14 | `binance_live_trader.py` يرجّع أرصدة وهمية بلا توقيع | لم يكن يتصل ببينانس أصلاً | نُقل إلى `legacy/` |
| 15 | الكاش يقبل أي ملف فيه >200 شمعة | طلب 730 يوماً يُرجع 30 | ميتاداتا + `covers()` + تحديث تزايدي |
| 16 | `sqrt(252)` للكريبتو | تسنيع خاطئ | 365 يوماً، مشتق من الإطار وموثّق |
| 17 | لا تحقق من أطوال مصفوفات OHLCV | انهيار غامض | `__post_init__` يرفض |
| 18 | جودة بيانات فيها 5 عيوب تحصل على 0.82 | بيانات معطوبة تمر | سقف صارم 0.55 للعيوب البنيوية |
| 19 | اشتراط efficiency و ADX معاً بـ AND | ADX=52 وميل +7.7% يُصنَّف TRANSITION | ميل + (كفاءة أو ADX قوي) |
| 20 | ادعاءات "PF 1.8، +$2450" بلا مصدر | غير قابلة لإعادة الإنتاج | حُذفت؛ كل رقم من `reports/` |

## بنود المواصفات 1–20 (عدا 19)

| البند | الحالة |
|---|---|
| 1 فحص المشروع | ✅ |
| 2 Look-Ahead | ✅ مُصلَح + اختبار آلي (اكتشف تسريبين) |
| 3 محرك باكتست واحد | ✅ Event-driven |
| 4 تنفيذ OHLC | ✅ + سياسة الشمعة المزدوجة + الفجوات |
| 5 إغلاق المركز عند النهاية | ✅ `BACKTEST_END` |
| 6 Walk-Forward حقيقي | ✅ تدريب/تجميد/OOS + استقرار المعاملات |
| 7 Score/Confidence/Probability | ✅ + Isotonic/Platt + Brier + Reliability |
| 8 النجوم | ✅ رتيبة، لا `%` |
| 9 بيانات بينانس حقيقية | ⚠️ الكود جاهز — **BLOCKED** في بيئة البناء (403) |
| 10 الكاش | ✅ ميتاداتا + ذرّي + تحديث تزايدي |
| 11 Multi-Timeframe | ✅ 5m→1D، شموع مغلقة فقط |
| 12 محرك مؤشرات واحد | ✅ 15+ مؤشراً |
| 13 هيكل السوق | ✅ HH/HL/LH/LL + اختراق/إعادة اختبار/فاشل |
| 14 حالة السوق | ✅ 7 حالات، عتبات من Config |
| 15 محرك الإشارات | ✅ BUY/WAIT/NO_TRADE، لا SHORT |
| 16 محرك المنع | ✅ 25 سبباً مُرمّزاً |
| 17 إدارة المخاطر | ✅ مخاطرة فعلية |
| 18 الحد اليومي | ✅ أساس ثابت |
| ~~19 مخاطر المحفظة~~ | مستثنى بطلبك |
| 20 دفتر الأوامر | ⚠️ `order_book()` و`ticker()` جاهزان — **BLOCKED** بلا شبكة |

## BLOCKED

**البند 9 و20 — بيانات السوق الحية**
`REASON`: بيئة البناء تحجب `api.binance.com` (403 / allowlist).
`SAFE FALLBACK`: `DataUnavailable` صريح. لا بيانات مولّدة. الكود صحيح
وسيعمل على جهازك.
