# V11_FINAL_AUDIT_REPORT — المراجعة النهائية الكاملة

مراجعة المشروع كلّه عبر خمس جولات. هذا المستند هو الحصيلة.

```
الاختبارات:  689 محرك + 140 لوحة + 42 واجهة  =  871   كلها تنجح
اختبار تحمّل: 14 يوماً مُسرَّعة، 84 دورة، PASS
المصدر:      ~14,900 سطر بايثون
```

---

## الحصيلة: أحد عشر بقاً

| # | الخطورة | البق | الجولة |
|---|---|---|---|
| 1 | 🔴 | محرك اختيار أفضل فرصة بلا أي مسار تشغيل | 1 |
| 2 | 🔴 | `self.symbol` لا ينجو من إعادة التشغيل — وقف المركز لا يُسوَّى أبداً | 1 |
| 3 | 🟡 | فحص تعرّض المحفظة يُتخطّى بصمت في الإنتاج | 1 |
| 4 | 🟡 | `already_has_open_position` ثابتة | 1 |
| 5 | 🔴 | **رسوم الدخول خارج حساب الربح** — انحراف عن الباكتست | 2 |
| 6 | 🟡 | وحدات الرسوم مختلطة (BTC يُطرَح كأنه دولارات) | 2 |
| 7 | 🟡 | نافذة عمياء في حماية الوقف | 2 |
| 8 | 🔴 | مركز مكشوف لا يُوقف التداول | 2 |
| 9 | 🔴 | **المصالحة تسجّل كل خسارة صفراً** — تجاوز لحدود المخاطر | 3 |
| 10 | 🟡 | `ALL_DONE` تُترجَم «تنفيذ وقع» ولم يقع | 3 |
| 11 | 🔴 | `BREAKOUT_RETEST_ENABLED=1` يعطّل النظام بصمت | 4 |

وفي هذه الجولة الأخيرة، بقّان إضافيان:

| # | الخطورة | البق |
|---|---|---|
| 12 | 🔴 | **الباكتست يفتح مراكز أكبر بـ 11.1% من التنفيذ الحيّ** |
| 13 | 🔴 | **حدود Live الصارمة غير موصولة بمسار المال الحقيقي** |

---

## 12. 🔴 الباكتست ينحرف عن الحيّ بـ 11.1% — وأنا سببته

بعد ربط المسار الحيّ بلقطة الحساب (احتياطي نقدي، قيود منصة، إعادة
حساب مخاطرة)، بقي الباكتست ينادي `PositionSizer` **بلا لقطة** فيسلك
المسار القديم. القياس لنفس الإشارة ونفس النقد:

```
الباكتست  qty=0.05000000  notional=2,500.00
الحيّ      qty=0.04499000  notional=2,249.50
                          ⛔ أكبر بـ 11.1%
```

هذا انحراف أحدثته أنا في جولة سابقة ولم ألاحظه حتى هذه المراجعة. وهو
**نفس صنف البق رقم 5 بالضبط**: مساران يحسبان الشيء نفسه بطريقتين،
فيُبالغ الباكتست في العوائد بلا أي سبب استراتيجي.

**الإصلاح**: `SimulatedAccountProvider` — حساب محاكى يمرّ بنفس مسار
التحجيم الحيّ تماماً (القسم 23 من المواصفة). و`tests/
test_backtest_live_parity.py` يحرس التكافؤ، بستة اختبارات منها واحد
يُثبت أن المسار القديم كان يُنتج حجماً أكبر فعلاً — فلو تساوى المساران
يوماً لعرفنا أن الاحتياطي توقّف عن العمل.

## 13. 🔴 طبقة أمان المال الحقيقي ليست في مسار المال الحقيقي

`LiveOrderManager` و`LiveLimits` مبنيّان ومُختبَران بالكامل — و
`LiveOrderManager(` **لا يظهر في أي ملف إنتاج**، في الاختبارات فقط.
`LiveTrader` يبني `OrderManager` العادي في كل الأوضاع بما فيها `live`.

الفارق ليس تجميلياً:

| | المُطبَّق فعلاً | `LiveLimits` |
|---|---|---|
| مخاطرة لكل صفقة | 1.00% | **0.25%** |
| سقف قيمة المركز | 25% من الحقوق | **$25 مطلقاً** |
| التعرّض الكلي | 60% | **10%** |

على حساب بـ 10,000 دولار: الحدود المكتوبة للمال الحقيقي تسمح بـ$25
للمركز، والمُطبَّق فعلياً يسمح بـ$2,500 — **مئة ضعف**.

Mainnet مقفول ببوابة الجاهزية، فالعيب **كامن لا نشط**. لكن الاعتماد
على قفل واحد لعيب بهذا الأثر لا يكفي، فأُضيف حاجز صريح مستقل: وضع
`live` يرفض الإقلاع بـ `LIVE_LIMITS_NOT_WIRED` ما لم يُقرّ المُشغِّل
بالتجاوز صراحةً.

**هذا بند حاجب قبل أي تفعيل لـ Mainnet.** الإصلاح الصحيح وصل
`LiveOrderManager` بمسار التنفيذ — وهو تغيير معماري لم أُقدِم عليه من
تلقاء نفسي لأن Mainnet مقفول ولأنه يتجاوز نطاق المراجعة.

---

## النمط البنيوي — خمس مرّات

خمسة من البقّان الثلاثة عشر (1، 11، 12، 13، وانحدار ربط BTC الذي
وثّقه كاتب V11 بنفسه) هي **نفس الخطأ**:

> مكوّن مبنيّ ومُختبَر وموثَّق بعناية — وغير موصول بالمسار الذي يعمل فعلاً.

الاختبارات تُوجَّه إلى المكوّنات، والإنتاج يمرّ عبر الأغلفة. هذه في
رأيي نقطة الضعف البنيوية في المشروع، لا أي بق منفرد.

**الدواء المُطبَّق**: كل ميزة أُصلحت في هذه الجولات لها الآن مسار تشغيل
حقيقي (علَم سطر أوامر أو أمر مستقل)، واختبار يمرّ عبر `main()` أو
`tick()` الحقيقية لا عبر استدعاء معزول.

---

## ما فُحص وكان سليماً

| المجال | النتيجة |
|---|---|
| حقن SQL | نظيف — الأعمدة داخلية والقيم دائماً `?` |
| الأسرار | لا طباعة لمفتاح قط؛ كل المواضع دوال تجزئة |
| `except:` عارية / وسائط متغيّرة / `eval` | صفر |
| قاعدة البيانات | WAL، `BEGIN IMMEDIATE`، `RLock`، 15 فهرساً، تراجع صحيح |
| طبقة البيانات | `DataUnavailable` بلا أي بديل اصطناعي، إسقاط الشمعة غير المُغلَقة |
| سياق BTC | سببي بحت، فشل مغلق عند الغياب |
| الباكتست | إشارة تُنفَّذ عند فتح الشمعة التالية؛ حدود المخاطر مُطبَّقة عبر نفس البوابة |
| نماذج الدخول | سببية — مُثبَت آلياً على المسار الحيّ |
| اللوحة | PBKDF2 بـ200 ألف دورة، مقارنة ثابتة الزمن، قاعدة للقراءة فقط بطبقتين، رفض صارم لـ DEBUG و CORS `*` خارج local |
| الوسيط الورقي | محافظ: الوقف يُنفَّذ بسعر أسوأ عند الفجوة، والهدف بسعره بالضبط |
| `watchdog` | سقف 10 إعادات في نافذة، بتراجع أُسّي |

---

## ملاحظات موثَّقة (ليست بقّاناً)

**عدّادا الأعطال في الذاكرة.** `api_failures` و`order_failures` في
`HealthMonitor` يصفران عند إعادة التشغيل. الحماية الأساسية محفوظة
(مفتاح الإيقاف في `kv`، الأحداث في `risk_events`، سقف إعادات التشغيل
في `watchdog`)، فهذه طبقة إضافية لا أساسية. موثَّق باختبار.

**الباكتست لا يتداول على بيانات اصطناعية.** 18 تشغيلاً بالإعداد
الافتراضي بلا صفقة واحدة. ليس بقاً — الاستراتيجية انتقائية والبيانات
ضوضاء. لكنه يعني أن أي اختبار يعتمد على صفقات يحتاج عتبات مُخفَّضة
صراحةً، وإلا تخطّى نفسه وأوهم بالتغطية.

**`_settle_after_stop` كود ميت** مُبقى عمداً وموثَّق.

---

## حدود هذه المراجعة — صريحة

**فُحص سطراً سطراً**: `order_manager.py` (850 سطراً)، `idempotency.py`،
`reconciliation.py`، `position_sizing.py`، `risk_guard.py`،
`backtest/engine.py`، `account/*`، `signals/no_trade.py`،
`market/btc_context.py`، `data/binance.py`، `monitoring/health.py`،
`live/live_order_manager.py`، `live/live_config.py`.

**فُحص موجَّهاً** (بحث عن أصناف أخطاء محدَّدة لا قراءة كل سطر):
`indicators/engine.py`، `market/{structure,regime,mtf}.py`،
`validation/*`، `monitoring/{gates,notify,accuracy,shadow}.py`،
`dashboard/backend/*`، الواجهة.

**لم يُفحص**: `legacy/` (مستبعد من الاختبارات وغير مستخدَم)، وسكربتات
توليد التقارير في الجذر.

---

## الحالة النهائية

```
ENGINE              PASS
ACCOUNT BALANCE     PASS (paper) / NOT VERIFIED (testnet, live)
POSITION SIZING     PASS
BEST OPPORTUNITY    PASS
OCO                 PASS
PAPER               PASS
RECONCILIATION      PASS
BACKTEST            PASS
DASHBOARD           PASS
SECURITY            PASS
FULL TEST SUITE     PASS (871)
MAINNET             LOCKED  ← وبند حاجب رقم 13 قائم
STRATEGY EDGE       NOT PROVEN
OVERALL             READY FOR PAPER
                    NOT READY FOR TESTNET (رصيد المنصة غير مُتحقَّق)
                    NOT READY FOR LIVE
```

**النظام لم يتداول ولا مرّة.** لا Paper حقيقي ولا Testnet. كل ما سبق
سلامة تنفيذ، ولا يقول شيئاً عن الربح. الخطوة التالية في
`RUNBOOK_14D_PAPER.md`.

---

## 14. 🟡 رصد نموذج الدخول — ثغرة وجدتها مراجعة الاستراتيجية

`Signal` كان **بلا حقل `setup_type` إطلاقاً**. المسار الحيّ لا يستطيع
الإجابة عن «أي نموذج أنتج هذه الصفقة؟».

والأسوأ من الغياب: أي قياس يقرأ
`getattr(sig, 'setup_type', 'baseline')` كان يُرجع `baseline` لكل شيء
**بصمت** — فتبدو النماذج الأخرى معطَّلة وهي تعمل.

وقعتُ في هذا بنفسي: قياسي الأول أعلن أن breakout وpullback «لا يفوزان
أبداً» عبر 1500 شمعة. القياس بعد الإصلاح:

```
الافتراضي         BUY=110   {BASELINE: 110}
breakout مُفعَّل  BUY=110   {BASELINE: 107, BREAKOUT: 3}
pullback مُفعَّل  BUY=110   {BASELINE:  67, PULLBACK: 43}
كلاهما            BUY=110   {BASELINE: 67, BREAKOUT: 3, PULLBACK: 40}
```

**46 صفقة من 110 لم تكن baseline** — وكانت تُسجَّل كذلك.

**الإصلاح**: حقل في `Signal`، ينتشر من `EntrySignal` عبر المحوّلين،
ويُخزَّن في عمود `signals.setup_type` (ترحيل v7). بلا التخزين يستحيل
لاحقاً الإجابة عن «هل يستحق breakout/pullback البقاء؟» من تشغيل حقيقي
بدل التخمين.

عشرة اختبارات في `tests/test_setup_type_visibility.py`، **كلها تفشل
على الشجرة قبل الإصلاح**. أهمّها حارس يسقط لو تطابق توزيع الإشارات مع
تفعيل النماذج وبدونه — أي لو عاد الوهم القديم.

**المجموع بعد هذا الإصلاح: 881 اختباراً** (699 + 140 + 42).
