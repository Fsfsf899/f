# POST_V11_AUDIT_ROUND2_REPORT — الرسوم وحماية الوقف

مراجعة ملفاً ملفاً لمسار التنفيذ في `crypto_trading_v11`. خمس مشاكل
مُصلَحة، أخطرها خطأ في حساب الربح يجعل التشغيل الورقي ينحرف عن الباكتست
بنيوياً.

## الحالة

| | قبل | بعد |
|---|---|---|
| `tests/` | 601 | **616** |
| `dashboard/tests/` | 140 | 140 |
| واجهة (`.mjs`) | 42 | 42 |
| **المجموع** | 783 | **798** |

الخمسة عشر اختباراً الجديدة في `tests/test_fees_and_stop_gap.py`.
**أحد عشر منها تفشل فعلياً على الشجرة قبل الإصلاح** (٨ `FAIL` + ٣
`ERROR`) — مُتحقَّق بتشغيلها على نسخة ما قبل الإصلاح. الأربعة الباقية
حُرَّاس «السلوك لم يتغيَّر».

---

## 1. 🔴 رسوم الدخول لا تدخل في الربح المحقَّق

سطران يحسبان الشيء نفسه بطريقتين:

```python
# الباكتست — src/backtest/engine.py:250
pnl = gross - pos.entry_fee - fill.fee          # الرسمان ✅

# التنفيذ الحي والورقي — order_manager._record_exit  (قبل الإصلاح)
pnl_chunk = (avg - pos['entry_price']) * qty - fee     # الخروج فقط ❌
```

**الإثبات** — مركز $1000، دخول وخروج بالسعر نفسه، بلا حركة سوق:

```
رسوم الدخول $1.00 | رسوم الخروج $1.00
PnL الصحيح:   -$2.00
PnL المُسجَّل: -$1.00      ← مُبالَغ فيه بمقدار رسم الدخول بالضبط
حقل fees:     $2.00      ← الرقم الصحيح مُخزَّن، لكنه لا يُستخدَم
```

**لماذا هو الأخطر**: التشغيل الورقي موجود ليتحقّق من الباكتست، والمساران
يستخدمان معادلتين مختلفتين. فـ Paper يُظهر دائماً نتائج أفضل من الباكتست
لنفس الصفقات، ويُقرأ الفارق كإشارة سوق وهو خطأ حسابي. وبرسوم 0.1% لكل
جهة، **نصف تكلفة التداول مخفيّ عن كل تقرير**.

**الإصلاح**: `entry_fee_unbilled` يُطرَح مرة واحدة فقط — في أول شريحة
خروج — حتى لا يتكرّر طرحه في خروج جزئي متعدد المراحل.

## 2. 🔴 المنصة الوهمية بعمولة صفر — السبب البنيوي

`tests/fake_exchange.py` كانت تُرجع `commission: '0.0'` دائماً، فلم يكن
بإمكان **أي** اختبار في المشروع أن يرى خطأ رسوم. هذا ما أخفى البند 1.

**الإصلاح**: `fee_rate` و`fee_asset` اختياريان. الافتراضي يبقى `0.0`
عمداً كي لا تتغيَّر توقّعات الاختبارات القائمة (٦٠١ اختباراً)، لكن
اختبارات الرسوم تُفعِّله بقيمة حقيقية. عند التفعيل يُحاكي سلوك بينانس
لا المبسَّط: **عمولة الشراء بالأصل الأساس، وعمولة البيع بالمقابلة**.

## 3. 🟡 وحدات الرسوم مختلطة

`normalize_response` كانت تجمع `commission` بلا نظر إلى
`commissionAsset`. بينانس تخصم عمولة الشراء من الأصل الأساس (BTC)،
بينما `paper_broker` يخصمها دائماً من المقابلة — فعلى المنصة الحقيقية
كان رقم بالبيتكوين يُطرَح كأنه دولارات. انحراف Paper↔Live لا يكشفه أي
اختبار للسبب في البند 2.

**الإصلاح**: `fee` صار مُقوَّماً بالعملة المقابلة دائماً. عمولة بالأصل
الأساس تُحوَّل بسعر التنفيذ نفسه. عمولة بأصل ثالث (BNB عند تفعيل الخصم)
**لا تُخمَّن**: تُستبعَد، ويُرفع `fee_uncertain`، ويُسجَّل حدث
`FEE_ASSET_UNCONVERTIBLE`، وتُحفَظ القيمة الخام في `fee_by_asset`.

## 4. 🟡 النافذة العمياء في حماية الوقف

في `guard_stops`: إعادة وضع الوقف تشترط `px > stop` (بينانس ترفض وقفاً
يُفعَّل فوراً)، والخروج الطارئ يشترط اختراقاً ≥ `emergency_breach_pct`.
فإذا اختفى الوقف والسعر بينهما — أقلّ من 0.5% تحت الوقف — لم يكن يحدث
**أي شيء**: لا وقف، لا خروج، ولا تسجيل.

**الإصلاح**: قسم `2ب` جديد. السعر تحت الوقف يعني أن شرط الخروج تحقَّق
فعلاً، فالخروج السوقي هو التصرّف الصحيح لا الانتظار. لا يُنفَّذ إلا بعد
التأكد من أن المركز قائم (`_verify_still_held`)، كما في المسار الطارئ.

## 5. 🟡 مركز مكشوف لا يُوقف التداول

اكتشفته اختبارات هذا الملف نفسها أثناء كتابتها. عند تأكُّد زوال الوقف من
المنصة وفشل إعادة وضعه، كان يُسجَّل `UNPROTECTED_POSITION` بخطورة
CRITICAL ثم **يتابع النظام التداول طبيعياً** — و`HealthMonitor` لا يمسح
`risk_events` حسب الخطورة إطلاقاً، فالحدث سطر في القاعدة بلا أثر تشغيلي.
مركز مكشوف، والنظام يفتح فوقه مراكز جديدة.

يخالف القاعدة المطلقة التي يقتبسها المشروع في `_record_exit` وينفّذها
هناك: *"If protection cannot be proven, the position is NOT protected
and new trading must be blocked"*.

**الإصلاح**: توحيد السلوك — مفتاح الإيقاف يُشتعل، كما في مسار الخروج
الجزئي تماماً. الحالة مؤكَّدة لا مشكوكة: الاستعلام أثبت زوال الوقف،
وإعادة وضعه فشلت.

## 6. افتراض بطل في `RiskGuard`

الوثيقة كانت تقول «النظام يُشغِّل رمزاً واحداً لكل عملية تشغيل» — ولم
يعد صحيحاً منذ محرك المسح متعدّد الرموز في V11. `ACCOUNT_WIDE` يبقى
الافتراضي الصحيح، بل صار **أكثر** صحةً: الحد اليومي وسلسلة الخسائر
خصائص محفظة لا رمز، ولو كان لكل رمز عدّاد مستقل لأمكن تجاوز الحد اليومي
بالتنقّل بين الرموز. صُحِّح النص وسُجِّل المبرَّر الجديد.

---

## ما فُحص وكان سليماً

| | |
|---|---|
| حقن SQL | لا شيء — كل `f-string` يُقحم أسماء أعمدة داخلية؛ القيم دائماً `?` |
| الأسرار | لا طباعة لمفتاح قط — كل المواضع دوال `fingerprint()` تجزئة |
| `except:` عارية | صفر |
| وسائط افتراضية قابلة للتغيير | صفر |
| مقارنة أموال بـ `==` | صفر |
| `eval` / `exec` / `pickle` | صفر |

قاعدة البيانات متينة: WAL، `BEGIN IMMEDIATE`، `RLock`، مهلة 30ث، 15
فهرساً، تراجع يُعيد رفع الخطأ الأصلي. `position_sizing` يحسب المخاطرة
الفعلية بعد التكاليف لا الاسمية. `order_manager` يفشل مغلقاً في كل
مسار فُحص عدا ما ورد أعلاه.

## حدود هذه المراجعة

فُحص بعمق: `order_manager.py` كاملاً (850 سطراً)، `oco.py`،
`paper_broker` (الرسوم)، `risk_guard`، `position_sizing`، `database`،
`signals/engine`، `cache`، `binance`، مع مسح آلي للشجرة كلّها.

**لم يُفحص بعمق**: `idempotency.py` و`reconciliation.py` (544 + 367
سطراً — منطق التعافي والمصالحة، وهو ما يبقى من مسار المال)، و`entry/*`،
واللوحة، و`validation/*`.

## ما لم يتغيَّر

ربحية الاستراتيجية **لا تزال غير مثبتة**، ولم يجرِ تشغيل Paper أو
Testnet حقيقي. هذه الإصلاحات تجعل أرقام ذلك التشغيل **صحيحة عند وقوعه**
— وهو شرط ضروري لا كافٍ.

---

# ملحق — الجولة الثالثة: `idempotency` و `reconciliation`

فُحص الملفان اللذان بقيا من مسار المال (911 سطراً). بقّان إضافيان.

| | قبل | بعد |
|---|---|---|
| `tests/` | 616 | **626** |
| **المجموع** | 798 | **808** |

العشرة الجديدة في `tests/test_recovery_and_recon.py`؛ **ستّة منها تفشل
على الشجرة قبل الإصلاح** (٢ `FAIL` + ٤ `ERROR`).

## 7. 🔴 المصالحة تُسجِّل كل خروج بربح صفر

`auto_resolve` كانت تُغلق أي مركز مفقود من المنصة بـ
`realized_pnl=0.0` **دائماً**. والحالة الشائعة لـ `POSITION_MISSING`
هي أن الوقف نُفِّذ فعلاً — أي خسارة حقيقية تُسجَّل صفراً.

**الإثبات**: مركز 0.02 BTC بوقف عند −$20 — الوقف نُفِّذ على المنصة:

```
قبل:  الربح المُسجَّل $0.00  | الربح الصحيح −$20.00 | سلسلة الخسائر 0 → 0
بعد:  الربح المُسجَّل −$20.00 | الربح الصحيح −$20.00 | سلسلة الخسائر 0 → 1
```

**الأثر ثلاثي**:
1. كل تقرير يعتبرها صفقة متعادلة — منحنى الأداء مزيَّف بالكامل.
2. `close_position()` لا تُحدِّث `RiskGuard`، فالخسارة لا تدخل الحد
   اليومي ولا سلسلة الخسائر — **حدود المخاطر تُتجاوَز عبر هذا المسار**،
   وهي آخر خط دفاع.
3. المصالحة تسبق `guard_stops()` في `tick()` (السطر 290 مقابل 315)،
   فإغلاقها المزيَّف **يمنع المسار الصحيح من تسجيل الخروج أصلاً**.

**الإصلاح**: يُقرأ الخروج الحقيقي من المنصة ويُسجَّل عبر `_record_exit()`
الكنسي نفسه — لا تطبيق موازٍ. وإن تعذّر إثبات الخروج، **لا يُغلَق المركز
بصفر مُختلَق**: يبقى الاختلاف مانعاً للتداول مع حدث
`RECON_EXIT_UNPROVEN`، وهو سلوك المشروع المعلن عند غياب اليقين.
`Reconciler` صار يستقبل `order_manager` اختيارياً؛ غيابه يجعل الامتناع
صريحاً بدل تزوير الرقم.

## 8. 🟡 `ALL_DONE` تُترجَم «تنفيذ وقع» ولم يقع

`_try_recover_oco` كانت تترجم `listOrderStatus == 'ALL_DONE'` إلى
`FILLED`. لكن `ALL_DONE` في بينانس تعني فقط أن القائمة لم تعد نشطة —
وتشمل **إلغاء الطرفين معاً** والمركز مفتوح تماماً.

```
أطراف OCO                        قبل        بعد
الوقف نُفِّذ، الهدف أُلغي         FILLED  →  FILLED   ✅
الهدف نُفِّذ، الوقف أُلغي         FILLED  →  FILLED   ✅
الطرفان أُلغيا — الحماية زالت     FILLED  →  CANCELED ✅
```

**الأثر**: سجل يقول إن تنفيذاً وقع ولم يقع. و`_send_loop` يعامل FILLED
كنجاح، فتُخزَّن معرّفات وقف وهدف **مُلغيين** على المركز فيبدو محمياً
وهو مكشوف.

**تصحيح لادّعاء سابق في هذه المراجعة**: قلت إن هذا يمنع إعادة الحماية
للأبد. **غير صحيح** — `active_intents()` تستبعد الحالات النهائية، فتُصدَر
نسخة وقف جديدة و`guard_stops()` تُعيد الحماية في دورة لاحقة. الضرر في
زيف السجل وفي نافذة الانكشاف، لا في استحالة التعافي. أحد الاختبارات
يوثّق هذا الحدّ صراحةً ويمرّ قبل الإصلاح وبعده.

**الإصلاح**: الحسم من حالات الأطراف (`orderReports`) لا من حالة القائمة
المجمَّعة، مع حدث `OCO_CANCELED_NOT_FILLED` عند زوال الحماية بلا تنفيذ.

## ما بقي غير مفحوص بعمق

`entry/*` (breakout/pullback)، واللوحة، و`validation/*`. مسار المال
صار مفحوصاً بالكامل.
